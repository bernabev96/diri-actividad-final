export const ROUTES = {
  home: '/',
  login: '/login',
  register: '/register',
  classes: '/classes',
  profile: '/profile',
  admin: '/admin',
  adminClasses: '/admin/classes',
  adminUsers: '/admin/users',
} as const;
